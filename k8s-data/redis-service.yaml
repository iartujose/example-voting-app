apiVersion: v1
kind: Service 
metadata: 
  name: redis
  labels:
    name: redis-service
    app: demo-voting-app
spec:
  ports:
    - port: 6379
      targetPort: 6379
      # No need to add NodePort since it won't be exposed, it's only internal
  selectors:
    name: redis-pod
    app: demo-voting-app